import React, { ReactElement } from 'react'

interface Props {
    
}

export default function StaffSearchHeader({}: Props): ReactElement {
    return (
        <React.Fragment>
            <h1>Header</h1>
        </React.Fragment>
    )
}
