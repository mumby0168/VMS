import React, { ReactElement } from 'react'

interface Props {
    
}

export default function StaffResults({}: Props): ReactElement {
    return (
        <React.Fragment>
            <h1>Results to go here.</h1>
        </React.Fragment>
    )
}
